# NAS 部署說明

給 IT 與執行者的設定說明。研究：BD-TS-20260701 Alu qPCR 生物分布統整。

## 一、部署步驟（依序做，約 20 分鐘）

### 步驟 1：在 NAS 建立資料夾

到 `\\10.2.18.80\05 ts\PT臨床前試驗中心\Edison Test\` 建立三個資料夾：

```
Edison Test\
├─ data\
│  ├─ raw\             原始 qPCR export (.xls)
│  └─ reference\       動物分組/性別/上機編號等參考檔
└─ output\             產出的統整表與對照報告
```

（`data\decisions.xlsx` 由步驟 4 自動產生，不用自己建。）

### 步驟 2：把資料複製到 NAS

* 9 個原始 `.xls` → `data\raw\`
* `BD-TS-20260701 上機編號_說明.xlsx` 等參考檔 → `data\reference\`

**用複製，不要用移動。** 原始檔在原處留一份。

### 步驟 3：更新本機的程式資料夾

重新下載 ZIP 覆蓋 `C:\qpcr-biod\`。新版設定檔的路徑已經指向 NAS，不用自己改。

> 想確認的話，打開 `config\BD-TS-20260701.yaml`，`paths` 四行應該都是
> `\\10.2.18.80\...` 開頭，而且用**單引號**包起來。

### 步驟 4：產生 NAS 上的決策表

雙擊 `安裝.bat`。它會在 NAS 的 `data\` 底下建立 `decisions.xlsx`。

> 已經裝過也要再跑一次 —— 這次是為了把決策表建到 NAS 上。
> 已存在的決策表不會被覆蓋。

### 步驟 5：跑一次，確認讀得到 NAS

雙擊 `執行統整.bat`。

* 應該看到 `讀入 run 數：9`、`最終採用檢體數：270`
* 輸出路徑應該是 `\\10.2.18.80\...\output\BD-TS-20260701_統整表.xlsx`

跑不動的話多半是網路磁碟權限或路徑打錯，錯誤訊息會指出是哪一個資料夾。

### 步驟 6：請 IT 設定權限

把下一節的權限表交給 IT。**在權限設好之前先不要通知同仁**，
否則他們可能會直接編輯 `output\` 裡的統整表，那些修改下次重跑就會消失。

### 步驟 7：通知同仁

把《同仁操作說明.md》發給大家。務必提醒第〇節的安全性提示 ——
第一次執行時 Windows 會跳出 Python 的許可通知，要按「同意」。

---

## 二、權限設定（請 IT 協助）

| 資料夾／檔案 | 實驗室同仁 | 執行者 | 理由 |
|---|---|---|---|
| `data\raw\` | **讀取** | 讀取＋寫入 | 原始儀器檔是唯一真實來源，不可被修改或刪除 |
| `data\reference\` | **讀取** | 讀取＋寫入 | 同上，屬受控的來源資料 |
| `data\decisions.xlsx` | **讀取＋寫入** | 讀取＋寫入 | 同仁記錄人工判斷的唯一入口 |
| `output\` | **讀取** | 讀取＋寫入 | 產出物，由程式重新產生，不應手動修改 |

**只有 `decisions.xlsx` 一個檔案需要開放同仁寫入。**

### 為什麼 raw 要設唯讀

這是 GLP 稽核會問的點。整套流程的可辯護性建立在「原始檔從未被更動」之上：

* 統整表的每個數字都可追溯到某個原始檔的某個孔位
* 產出檔的「執行紀錄-輸入檔」分頁記錄了每個原始檔的 SHA-256，
  任何改動都會讓雜湊值對不上而被發現
* 程式本身只讀不寫原始檔（有自動化測試守著這條不變式）

把 raw 設為唯讀，是讓檔案系統也一起保證這件事，而不是只靠程式自律。

## 三、每位使用者要做的事

### 同仁（只看表、填決策）

不需要安裝任何東西。

1. 開 `output\BD-TS-20260701_統整表.xlsx` 看最新結果
2. 需要記錄人工判斷時，編輯 `data\decisions.xlsx`
3. 詳見《同仁操作說明.md》

### 執行者（負責重跑）

1. 本機安裝一次（見《實裝測試指南.md》階段 A）
2. 之後每次重跑：雙擊 `執行統整.bat`

**第一次執行時 Windows 會跳出 Python 的安全性許可通知，請按「同意」。**
這是公司的應用程式控制原則對 pandas/numpy 的編譯檔要求授權，屬正常流程。

## 四、為什麼程式放本機而不是 NAS

兩種做法都可行，附的 `.bat` 都支援（使用 `pushd` 而非 `cd /d`，
所以從 UNC 路徑執行也不會失敗）。建議放本機的理由：

* 從網路磁碟執行 Python 有額外的應用程式控制風險，本機較單純
* 執行速度較快，不受網路狀況影響

代價是：**每位執行者各有一份設定檔，可能產生版本落差**。
若執行者超過一人，建議改為把整個專案資料夾放上 NAS，
讓所有人跑同一份 `config\BD-TS-20260701.yaml`。

## 五、注意事項

* **不要兩人同時執行。** 程式會覆寫 `output\` 裡的統整表，
  同時執行會互相蓋掉。實務上由單一執行者負責即可。
* **新增資料就是把檔案丟進 `data\raw\` 後重跑**，
  不需要標記哪些檔案處理過——每次都從全部原始檔重新計算。
* **設定檔的 UNC 路徑必須用單引號。** YAML 的雙引號會把反斜線
  當跳脫字元，寫成 `"\\10.2.18.80\..."` 會直接解析失敗。
