# 實裝測試指南

從「程式在 GitHub 上」到「同仁在用」的完整步驟。

導入分三個階段，**請照順序做完再進下一階段**：

| 階段 | 目的 | 資料 | 大約時間 |
|---|---|---|---|
| A. 裝起來 | 確認程式在你的電腦上跑得動 | 內建測試資料 | 15 分鐘 |
| B. 對帳 | 確認能重現你既有的統整表 | 一批真實資料 | 半天～一天 |
| C. 上線 | 同仁開始用 | 全部資料 | — |

**在 B 完成前，不要讓同仁使用產出的統整表。**

---

## 階段 A：裝起來（先在自己電腦上，不要直接裝在 NAS）

### A-1. 安裝 Python

到 <https://www.python.org/downloads/> 下載 Python 3.10 以上版本。

安裝時**務必勾選 `Add Python to PATH`**（在安裝畫面最下面）。漏勾的話後面每一步都會失敗。

### A-2. 取得程式

到 GitHub 專案頁 → 切換到分支 `claude/qpcr-integration-project-l224bk` →
綠色 `Code` 按鈕 → `Download ZIP`。

解壓縮到一個**路徑不含中文、不含空格**的地方，例如：

```
D:\qpcr-biod\
```

> 路徑有中文或空格不一定會壞，但一旦出問題會很難查。導入期先避開。

### A-3. 執行安裝

在該資料夾裡雙擊 **`安裝.bat`**。

它會依序：檢查 Python → 安裝套件 → 建立資料夾 → 產生決策表 → 跑自我測試。

**看到「安裝完成」才算成功。** 自我測試那一步會跑 47 項測試，如果沒有全部通過，
先不要繼續，把訊息截圖回報。

### A-4. 用內建測試資料跑一次

專案內附了兩個模擬的 StepOnePlus export，可以先確認整條流程通。

```
tests\fixtures\  裡的兩個 .xls
```

把它們複製到 `data\raw\`，然後在 `data\reference\` 放一個 Excel，
內容如下（欄名要一致）：

| Animal No | Sex | Time point |
|---|---|---|
| 0006 | Female | Predose |
| 1006 | Male | Predose |
| 1011 | Male | Day 2 |
| 1012 | Male | Day 2 |
| 1013 | Male | Day 2 |

雙擊 **`執行統整.bat`**，應該會看到「需人工複核列數：1」和輸出路徑。

打開 `output\BD-TS-20260701_統整表.xlsx` 確認：

- [ ] 「說明」分頁有產生時間
- [ ] 「統整數據」有資料，且有一列是淺紅底（1013_22，請人工複核）
- [ ] 「執行紀錄-輸入檔」有兩個檔案和它們的 SHA-256

到這裡代表程式本身沒問題。**接著把 `data\raw\` 和 `data\reference\` 清空**，
準備放真實資料。

---

## 階段 B：對帳（這一步最重要）

目標：證明 pipeline 能重現你已經人工確認過的 `BD-TS-20260701_Alu_qPCR_統整表.xlsx`。

### B-1. 放入真實資料

- 把 QPS BioD 資料夾裡那 9 個原始 `.xls` 複製到 `data\raw\`
- 把 3 個參考檔（Tissue Weight、Bone marrow cell pellet weight、上機編號_說明）
  複製到 `data\reference\`

**用複製，不要用移動。** 原始檔留在原處。

### B-2. 先試跑，確認讀得進去

雙擊 **`執行統整.bat`**。

這一步最可能出現的三種狀況：

**狀況 1：報錯「找不到表頭列」**
→ 代表原始 `.xls` 的實際格式跟預期不同。把錯誤訊息和一個原始檔給我，我調整 parser。

**狀況 2：跑完了，但警告很多**
→ 正常。導入期本來就會有一堆「臟器代碼未收錄」「性別缺漏」之類的提示。
先看過一遍，判斷哪些是真問題。

**狀況 3：跑完了，警告說性別/時間點全部留白**
→ 代表參考檔的欄名跟設定檔的別名對不上。打開參考檔看實際欄名叫什麼，
告訴我，我加進 `config\BD-TS-20260701.yaml` 的 `reference.column_aliases`。

### B-3. 對帳

雙擊 **`對照驗收.bat`**，把你既有的統整表 `.xlsx` 拖進視窗，按 Enter。

它會產生 `output\BD-TS-20260701_對照報告.xlsx`，裡面有：

- **對照摘要**：一致 / 有幾列差異
- **逐欄差異**：哪一筆、哪個欄位、舊表多少、pipeline 多少、差多少
- **只在既有表 / 只在pipeline**：兩邊列數對不上的部分

### B-4. 逐筆確認差異

**差異不代表 pipeline 錯。** 差異來源通常是這四類，處理方式不同：

| 差異類型 | 怎麼判斷 | 怎麼處理 |
|---|---|---|
| 舊表有未記錄的人工調整 | 舊表的值不是任何一孔也不是兩孔平均 | 把該筆判斷補進 `decisions.xlsx` |
| 標準品標稱濃度設錯 | LLOQ 或整批濃度系統性偏移 | 改設定檔 `nominal_concentrations` |
| rerun 認定不同 | 「版本」或「最終採用」欄位不一致 | 告訴我你們實際的 rerun 命名慣例 |
| pipeline 真的算錯 | 以上都不是 | 回報給我，附上該筆的對照報告列 |

**階段 B 的對帳已經先做過一輪了。**

用你的 9 個原始 run 與官方對照表跑完整 pipeline，對照既有統整表的結果：

| 對照項目 | 結果 |
|---|---|
| 統整數據 275 列 | **275/275 完全一致**（數值、ND、最終採用、孔位、性別、組別、時間點） |
| 標準曲線 9 條 | **9/9 一致**（slope、intercept、R²、PCR 效率） |
| 血液 LOB | **完全吻合**（n=20、Mean 0.261961、SD 0.151157、LOB 0.510615） |

需要注意的是：這一輪的逐孔輸入是從既有統整表的 Raw_Import 分頁重建的，
所以它驗證的是「統整邏輯」。**檔案解析層另外用一個真實的 .xls 單獨驗過**
（Run09，30 列數值與 Ct Mean 全部吻合到小數點後 8 位）。
你在自己電腦上跑階段 B 時，會是兩層一起驗，那才是完整的驗收。

### 對帳時請特別看這三個地方

1. **標準曲線與QC 分頁的 n、Slope、R²**。你既有表的 Run01 是
   `n=8, slope=-3.4769, R²=0.9984, 效率 93.9%`，pipeline 應該要算出同樣的數字。
   對不上代表回歸基準或標準品濃度還有問題。
2. **統整數據的「版本」與「最終採用」欄**。特別是同一塊盤上有兩個版本的檢體。
3. **批次與上機編號進度表**。標示為「依實際匯入資料逆推」的列都要人工核對——
   那些不是官方對照表明文排定的。

### B-5. 重複到乾淨

修正 → 重跑 → 再對帳，直到剩下的差異你都能解釋。

理想結果是「完全一致」，但**「有差異但每一筆都解釋得出來」也是可以接受的驗收標準** ——
只要那些差異是舊表當初的人工判斷，而且都補進決策表了。

---

## 階段 C：上線

### C-1. 決定放哪

程式和資料建議一起放在 NAS 的同一層，例如：

```
\\NAS\實驗室\qPCR統整\
├─ config\
├─ data\raw\
├─ data\reference\
├─ data\decisions.xlsx
├─ output\
├─ 執行統整.bat
└─ 同仁操作說明.md
```

`config\BD-TS-20260701.yaml` 的 `paths` 區塊可以直接填 UNC 路徑，
所以程式放本機、資料放 NAS 也可以。

### C-2. 權限怎麼開

| 對象 | data\raw | data\decisions.xlsx | output | config、程式 |
|---|---|---|---|---|
| 同仁 | 唯讀 | **可編輯** | 唯讀 | 唯讀 |
| 執行者（你） | 可寫 | 可編輯 | 可寫 | 可寫 |

`data\raw` 對同仁設唯讀是刻意的——原始檔是唯一真實來源，不該有人能改它。

### C-3. 誰按重跑

依你選的模式，同仁只看表和填決策，重跑由你或指定的人執行。三種做法：

- **手動**：同仁填完決策通知你，你雙擊 `執行統整.bat`
- **固定時段**：每天早上跑一次，用 Windows 工作排程器叫 `執行統整.bat`
- **有新檔就跑**：需要額外設定，先不建議

導入初期建議用手動，你會想親眼看每次的警告。

### C-4. 給同仁的說明

把 `同仁操作說明.md` 印出來或轉成 PDF 發給大家。那份是寫給不碰程式的人看的。

---

## 日常維運

| 情況 | 做法 |
|---|---|
| 有新的 qPCR 資料 | 丟進 `data\raw\`，重跑。不用管哪些檔案跑過了 |
| 同仁填了決策 | 重跑即可 |
| 要改允收標準、LOB 倍數、臟器代碼 | 改 `config\*.yaml`，不用改程式 |
| 想確認會不會出錯但不想覆蓋現有統整表 | 用 `check` 模式：`python -m qpcr_biod.cli check -c config\BD-TS-20260701.yaml` |
| 要證明某份統整表是怎麼來的 | 看該檔的「執行紀錄-輸入檔」分頁，比對原始檔 SHA-256 |

---

## 遇到問題要回報時

請附上這三樣，我才判斷得出問題在哪：

1. 執行畫面的**完整訊息**（整個視窗截圖，不要只截錯誤那一行）
2. 對照報告 `output\BD-TS-20260701_對照報告.xlsx`（如果是對帳階段）
3. 一個能重現問題的原始 `.xls`（如果是讀檔問題）
