# 安裝說明（給非工程背景的使用者）

這份說明假設你完全沒寫過程式。照著做就好，遇到不確定的地方，
最後一節「常見狀況」多半有答案。

**整件事的原理**：這套程式會在你的筆電上開一個「內部網頁」，
同仁在自己電腦的瀏覽器輸入一個網址就能用，他們**不必安裝任何東西**。
你的筆電就像公司內部的一台小主機，所以**它開著、程式開著，同仁才連得上**。

---

## 第一部分：安裝（只做一次，大約 15 分鐘）

### 步驟 1：安裝 Python

Python 是這套程式的執行環境，免費，由官方網站提供。

1. 開啟 <https://www.python.org/downloads/>
2. 按黃色的 **Download Python 3.x.x** 按鈕
3. 執行下載的安裝檔
4. **最重要的一步**：安裝畫面最下方有一個小方框
   **「Add python.exe to PATH」，一定要打勾**，再按 `Install Now`

   （沒勾到的話，之後會出現「找不到 Python」。補救方法是重新執行
   安裝檔，選 `Modify`，或乾脆移除後重裝一次並記得打勾。）
5. 出現 `Setup was successful` 就完成了，按 Close

### 步驟 2：把程式放到筆電上

1. 開啟程式碼頁面：<https://github.com/edison300trineo-droid/Claude-code->
2. 按綠色的 **Code** 按鈕 → **Download ZIP**
3. 下載完成後，在 ZIP 檔上按右鍵 → **解壓縮全部**
4. 把解壓縮出來的資料夾**改名為 `案件追蹤`，放到 `C:\` 底下**

   最後你應該會有這樣的路徑：`C:\案件追蹤\start.bat`

   （放桌面也可以，但路徑會很長，日後說明別人比較麻煩。）

### 步驟 3：第一次啟動

1. 打開 `C:\案件追蹤` 資料夾，找到 **`start.bat`**，**連按兩下**
2. 可能會跳出「Windows 已保護您的電腦」→ 點 **其他資訊** → **仍要執行**
   （這是因為檔案來自網路，不是病毒）
3. 接著跳出防火牆詢問 → **勾選「私人網路」** → 按 **允許存取**
   **這一步沒做，同仁會連不進來。**
4. 會出現一個黑色視窗，接著瀏覽器自動打開案件清單畫面

黑色視窗裡會顯示類似這樣的字：

```
案件追蹤系統 v1.0.0
  資料庫　：C:\案件追蹤\data\cases.db
  本機開啟：http://127.0.0.1:8765/
  同仁連線：http://192.168.1.23:8765/      ← 發這個給同仁
  　或用　：http://TMT-NB01:8765/          ← 這個更穩定，優先用這個
  停止服務：關閉此視窗，或按 Ctrl+C
```

### 步驟 4：先放示範資料看看（可跳過）

想先看看畫面長怎樣、有 7 筆假資料可以玩：
關掉黑色視窗，在 `C:\案件追蹤` 資料夾的空白處按住 `Shift` + 滑鼠右鍵
→ 「在此處開啟 PowerShell 視窗」，輸入下面這行後按 Enter：

```
python run.py seed-demo
```

再連按 `start.bat` 即可。這些假資料可以在畫面上一筆筆刪掉。

### 步驟 5：把網址發給同仁

發這段話給大家就可以了：

> 案件追蹤系統可以用了，請用瀏覽器開這個網址（加到我的最愛）：
> http://TMT-NB01:8765/
> 不用安裝任何程式。第一次進去請在右上角「操作者」填自己的名字。

（把 `TMT-NB01` 換成你黑色視窗上顯示的那一組。）

---

## 第二部分：每天怎麼用

| 情況 | 該怎麼做 |
| --- | --- |
| 早上到公司 | 開機後連按 `start.bat`，黑色視窗開著就好 |
| 那個黑色視窗 | **不能關掉**。關掉＝服務停止＝同仁連不進來 |
| 想暫停服務 | 關掉黑色視窗即可 |
| 你要離開座位 | 筆電**別關機、別闔上蓋子**（闔上通常會睡著，服務就停了） |
| 下班 | 關機沒關係，隔天再按一次 `start.bat`。資料都存在檔案裡，不會不見 |

### 讓它開機就自動啟動（建議設定）

1. 在 `start.bat` 上按右鍵 → **顯示其他選項** → **傳送到** → **桌面（建立捷徑）**
2. 按鍵盤 `Win + R`，輸入 `shell:startup` 按 Enter，會打開一個資料夾
3. 把剛剛桌面上那個捷徑**拖進這個資料夾**

以後開機就會自動跑起來，你只要不關那個黑色視窗即可。

### 避免筆電睡著（建議設定）

`設定` → `系統` → `電源` → 把「螢幕和睡眠」的**睡眠時間都設為「永不」**，
並確認插著電源。筆電一睡著，同仁就會連不上。

---

## 第三部分：把現有的 Excel 表格匯進來

如果你們原本就有一份 Excel 在追案件，不用重打：

1. 在網頁上按工具列的 **「匯入 Excel／CSV」**
2. 按 **選擇檔案**，挑那份 Excel（`.xlsx`）
3. 畫面會先顯示**預覽**：哪幾列會新增、哪幾列會更新、有沒有哪一列有問題，
   以及它認出了哪些欄位。**這個階段還沒有寫入任何東西**
4. 確認沒問題，按 **確認匯入**

幾件常見的事：

- **表頭不必在第一列**。上面有公司標題、製表人、空白列都沒關係
- 欄位順序不用調，用不到的欄位（金額之類）它會列出來但不會匯入
- 欄名可以是你們原本的寫法，例如「報價單號」「預計完成日」「計畫主持人」
- 日期不管是 `2026/9/20` 還是民國年 `115/1/15` 都認得
- **案件編號必須是 QT 開頭**，不符的列會被列出來，其他列照常匯入
- 同一個案件編號再匯入一次是**更新**，不會變成兩筆
- 舊版 `.xls` 不行，請先用 Excel 另存成 `.xlsx`

建議第一次匯入前，先照下一節的方式**備份一次 `cases.db`**，
萬一匯錯了可以直接還原。

---

## 第四部分：備份（很重要）

所有案件資料都在這一個檔案裡：

```
C:\案件追蹤\data\cases.db
```

**每週把這個檔案複製一份到公司網路磁碟或 OneDrive**，就是完整備份了。
程式執行中複製也沒問題。要還原時，把備份檔複製回原位覆蓋即可。

---

## 第五部分：常見狀況

**同仁說連不上**

依序檢查：

1. 你的黑色視窗還開著嗎？（最常見的原因）
2. 你的筆電是不是睡著了、或蓋子闔上了？
3. 同仁跟你連的是**同一個公司網路**嗎？（用手機熱點、在家連 VPN 都不行）
4. 網址有沒有打錯？結尾的 `:8765` 不能漏
5. 防火牆那一步是不是按到「取消」了？
   → `設定` → `隱私權與安全性` → `Windows 安全性` → `防火牆與網路保護`
   → `允許應用程式通過防火牆`，找到 Python 並勾選「私人」

**昨天的網址今天不能用了**

`192.168.x.x` 這種數字網址可能會變。改用黑色視窗上「或用」那一行的
**電腦名稱網址**（例如 `http://TMT-NB01:8765/`），它不會變。

**出現「找不到 Python」**

Python 安裝時「Add python.exe to PATH」沒打勾。重新執行安裝檔補裝一次。

**出現 `Address already in use` 之類的字**

代表已經有一個在跑了（可能你按了兩次 `start.bat`）。
把多餘的黑色視窗關掉，留一個就好。

**畫面打不開／怪怪的**

在瀏覽器按 `Ctrl + F5` 重新整理。建議用 Chrome 或 Edge。

---

## 第六部分：兩件你應該知道的事

**1. 沒有帳號密碼。** 公司網路內連得到這個網址的人都能編輯案件。
以內部使用來說通常沒問題，但請不要把它開放到公司以外的網路。
每一筆修改都有紀錄（誰、什麼時候、改了哪個欄位），可以追。

**2. 筆電當主機是「先跑起來」的做法，不是長期解。**
筆電會被你帶回家、會睡著、會關機，同仁那時就用不了。
如果用得順、大家開始依賴它，建議之後搬到一台**不關機的桌機或公司內部伺服器**——
搬家很簡單：把整個 `案件追蹤` 資料夾（含 `data\cases.db`）複製過去，
在新機器上裝 Python、按 `start.bat` 就好，資料完全不用轉檔。

---

## 附錄：Mac 使用者

Mac 已內建 Python，步驟更少：

1. 下載 ZIP 並解壓縮到「文件」資料夾，改名為 `案件追蹤`
2. 打開「**終端機**」（在 `應用程式 → 工具程式` 裡），貼上這兩行、按 Enter：

```
cd ~/Documents/案件追蹤
python3 run.py --open
```

3. 第一次執行會問是否允許連入網路連線，選 **允許**
4. 終端機視窗會顯示要發給同仁的網址，這個視窗一樣**不能關**

之後每次要啟動，重複第 2 步的兩行指令即可。
