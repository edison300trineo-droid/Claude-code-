# qPCR 統整表 — 同仁操作說明

給實驗室同仁看的版本。不需要懂程式。

---

## 〇、第一次執行時會跳出的安全性提示（重要）

第一次執行時，Windows 可能會跳出一個**關於 Python 的安全性許可通知**。

**請按「同意」／「允許」。**

原因：統整程式用到的 pandas、numpy 這兩個套件帶有編譯過的檔案，公司的應用程式
控制原則會先問過你才放行。這是正常流程，不是程式有問題。

> **如果不小心按了拒絕或關掉視窗**，程式會出現一長串 `ImportError ... DLL load
> failed` 的錯誤。這時只要重新執行一次，等提示再出現，按同意即可。

這個提示通常只會出現一次。日後套件更新時可能會再問一次，一樣按同意。

---

## 一、我要看最新的統整表

直接開共用資料夾裡的：

```
output\BD-TS-20260701_統整表.xlsx
```

這份檔案每次重跑都會整份重新產生。

**請不要直接修改這份檔案**——你改的內容下次重跑就會不見。
要調整內容請走下面的第二點。

### 看表時先確認兩件事

1. 翻到「**說明**」分頁，看最上面的「產生時間」，確認你看的是最新版。
2. 翻到「**執行紀錄-警告**」分頁，看有沒有需要處理的提示。

### 統整數據的底色代表什麼

| 底色 | 意思 | 要做什麼 |
|---|---|---|
| 淺紅 | 兩重複孔差異過大且沒有複測，需要人工判斷用哪一孔 | 需要你決定，見第二點 |
| 淺綠 | 同樣是孔位差異大，但結果是 ND | 屬預期現象，不用處理 |
| 淺灰 | 這一列不是最終採用（例如已被 rerun 取代），保留供追溯 | 不用處理 |
| 無底色 | 正常 | 不用處理 |

---

## 二、我要更新內容（填決策表）

所有人工判斷都填在這一個檔案：

```
data\decisions.xlsx
```

這是**唯一**需要人工編輯的檔案。填完之後請通知負責重跑的同仁，
或等排程自動重跑，內容就會反映到統整表上。

### 四個分頁分別在什麼時候用

**① 最終採用覆核**
同一隻動物的同一個臟器有多個結果（原始 + 複測）時，指定要採用哪一筆。

> 不填的話：系統預設採用最新的那次複測，其餘保留但標為非最終。
> 只有在你認為預設不對時才需要填。

> **同一個原始檔裡有兩個版本時**（例如同一塊盤上同時有 `1014_22` 和 `1014_22_re`），
> 光靠動物編號和來源檔案分不出你要指哪一個，請在「Sample Name」欄填上完整名稱。
> 該檔只有一個版本時留空即可。

**② HIGHSD單孔採用**
統整表上標成淺紅、寫著「請人工複核」的那幾列，在這裡指定要用哪一孔。

> 系統不會自己選孔，因為儀器匯出的檔案沒有附擴增曲線圖，
> 程式無法判斷哪一孔才是對的——這需要你看過原始曲線才能決定。
> 填「孔位1」「孔位2」或「兩孔平均」。
> 同一個原始檔裡有兩個版本時，同樣要在「Sample Name」欄指定是哪一個。

**③ 動物分組指定**
時間點無法唯一判斷組別時（例如 Day 29 兩組都可能採樣），在這裡指定該動物屬於哪一組。

**④ 臟器代碼補充**
出現了對照表裡沒有的臟器代碼時，在這裡補上名稱。

### 填寫時的四個注意事項

1. **動物編號要保留前面的 0**（`0006`，不是 `6`）。欄位已設成文字格式，正常打就好。
   （就算 Excel 幫你把 `0006` 變成 `6`，程式也會自動補回來。）
2. **覆核者和覆核日期一定要填。**
   少填任一個，那一列就不會生效，並且會在執行時列出提示。
   這是刻意的——沒有人簽名的判斷不應該進到報告裡。
3. **理由建議填。** 這欄會原樣出現在統整表的備註欄，之後查核時看得到。
4. **只填你要改的那幾列**，其餘留空。

---

## 三、我要重跑產生新版

在 Windows 上，雙擊：

```
執行統整.bat
```

跑完會顯示：讀了幾個 run、幾筆最終採用檢體、有幾列需要人工複核、輸出檔在哪、
以及所有警告。看完按任意鍵關閉。

如果畫面顯示「**有 N 列標記為請人工複核**」，代表有 N 筆需要你到決策表第 ② 分頁指定孔位。

---

## 四、有新的 qPCR 資料進來

把新的 `.xls` 檔案丟進：

```
data\raw\
```

然後重跑（第三點）。就這樣，不需要標記哪些檔案已經處理過——
每次都是從全部原始檔重新計算，所以不會有漏匯入或重複匯入的問題。

新的動物分組／性別／上機編號等參考檔，則放進 `data\reference\`。

---

## 五、常見狀況

**Q：警告說某個檔案「未含 HIGHSD 欄位」，是資料掉了嗎？**
不是。儀器只有在整盤存在 HIGHSD=Y 的孔位時才會匯出這個欄位；
整盤都是 N 的時候本來就不會有這一欄，屬正常現象。

**Q：警告說某隻動物的性別在不同參考檔不一致。**
程式不會擅自選一個，會留白並提示你。請確認哪一份參考檔正確，
更正檔案後重跑。

**Q：我填了決策表但數字沒變。**
先檢查覆核者和覆核日期是不是都填了（缺一不生效），
再看執行畫面或「執行紀錄-警告」分頁有沒有相關提示——
被忽略的決策一定會有一則提示說明原因。

**Q：可以直接改統整表裡的數字嗎？**
不行，下次重跑會被覆蓋。請改走決策表；
如果是規則本身要調整（例如允收標準），請找負責維護的同仁改設定檔。

**Q：我想知道某個數字是怎麼算出來的。**
統整數據裡有「來源檔案」和「孔位1/孔位2」的 Well 編號，
可以到「Raw_Import」分頁對回原始的逐孔資料。
「執行紀錄-輸入檔」還記錄了每個原始檔的 SHA-256，可以驗證檔案沒有被動過。
